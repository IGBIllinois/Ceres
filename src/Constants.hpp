
#pragma once

#include <cmath>


namespace nConstants
{
	const static double PI = std::asin(+1.0);

	const static double RAD_TO_DEG = PI / 180.0;
	const static double DEG_TO_RAD = 1.0 / RAD_TO_DEG;
}